# Kata TDD experience

Small repo that i use to store my progression through TDD experimentation.

## How to use

1. Dependencies
   Install dependencies with `npm install` or `pnpm install`

2. Launch tests
   Run `pnpm test` or `npm run test` to try test out in your terminal.
